#3) Modifique o programa abaixo para imprimir de 1 até o número digitado pelo usuário, mas, dessa vez, apenas os números ímpares.

start = 1;
end = int(input("Insira um numero inteiro: "))

while start <= end:
    print(start)
    start = start+2
